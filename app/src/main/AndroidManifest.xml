<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.relevamiento">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GrabadoraAudio"></activity>
        <activity android:name=".AyudaPlanilla" />
        <activity android:name=".CambiarDiagramas" />
        <activity android:name=".DiagramaCompleto" />
        <activity android:name=".Planilla" />
        <activity android:name=".Marcar" />
        <activity android:name=".Principal" />
        <activity android:name=".CrearCargarProyecto" />
        <activity android:name=".ListadoProyectos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>